import { OMExcludePipePipe } from './omexclude-pipe.pipe';

describe('OMExcludePipePipe', () => {
  it('create an instance', () => {
    const pipe = new OMExcludePipePipe();
    expect(pipe).toBeTruthy();
  });
});
