export interface EUIFilterObj {
    startDate: Date,
    endDate: Date,
    sourceId: number,
    departmentId: number
    statusId: number,
    daysToTarget:string,
    regionId: number,
    siteId:number,
}

export interface EUInterface{
    
}